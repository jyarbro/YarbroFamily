@page
@model App.Areas.Homes.Pages.Features.DeleteFeatureLevelModel
@{ ViewData["Title"] = "Delete Feature Level"; }

<p><a class="btn btn-sm btn-light btn-back-arrow" asp-page="./EditFeature" asp-route-id="@Model.FeatureId">Back</a></p>

<div class="p-2">
    <h5>Are you sure you want to delete this feature level?</h5>
    
    <p class="h6">@Model.Title</p>

    <div class="pt-4">
        <form method="post">
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="FeatureId" />
            <input type="submit" value="Yes, delete it." class="btn btn-danger" />
        </form>
    </div>
</div>
